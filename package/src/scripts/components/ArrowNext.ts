import type VanillaCalendar from '@src/vanilla-calendar';

const ArrowNext = (self: VanillaCalendar) => `<button type="button" class="${self.CSSClasses.arrowNext}" data-vc-arrow="next"></button>`;

export default ArrowNext;
