declare const errorMessages: {
    notFoundSelector: (selector: HTMLElement | string) => string;
    notInit: string;
    notLocale: string;
    incorrectTime: string;
};
export default errorMessages;
