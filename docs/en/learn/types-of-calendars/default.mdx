

# Default (Single)

The `'default'` calendar type displays one month, allows you to select days, switch between months using the arrows, and select the month and year from the respective headings.

<Sandbox example="type-of-calendar-default" />
