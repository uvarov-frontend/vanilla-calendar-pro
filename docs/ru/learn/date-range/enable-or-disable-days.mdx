# Включить или отключить дни

Иногда вам нужно будет отключить определенные дни для выбора, см. пример ниже.

<Info>
  Важно понимать, что в приведенных ниже примерах параметры `settings.selected.year` и `settings.selected.month` указаны только для демонстрации определенного месяца.
</Info>

<Sandbox example="date-range-disable-days" />

Давайте рассмотрим пример, где вам проще включить определенные дни, чем указывать список отключенных дней.

<Sandbox example="date-range-enable-days" />
