# Даты начала и окончания существующих дат

Важно знать, что диапазон дат в календаре можно задать двумя способами, сейчас мы поговорим о первом способе `date.min` и `date.max`. Эти два параметра указывают на допустимый диапазон дат в календаре.

По умолчанию в качестве минимальной даты используется `'1970-01-01'`, день начала <a href="https://en.wikipedia.org/wiki/Unix_time" rel="noreferrer" target="_blank">UNIX времени</a>.

Максимальная дата по умолчанию `'2470-12-31'` - эта дата не имеет символического значения, я ее просто выдумал. 🙂

Замените значения своими датами, если это важно для вас. Но помните, что даты за пределами указанного диапазона **не обрабатываются календарем**.

<Sandbox example="date-range-start-and-end-of-existing-dates" />
