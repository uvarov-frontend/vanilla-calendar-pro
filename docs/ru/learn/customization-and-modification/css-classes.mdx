# CSS-классы

Все классы CSS, используемые в календаре, являются переменными, которые можно настроить, заменив их собственными значениями. Однако следует учитывать, что при такой замене вам также потребуется самостоятельно добавить стили для измененного CSS-класса.

Ниже приведен пример замены класса для стрелок на собственный. Полный список классов можно найти в справочнике.

<Sandbox example="customization-and-modification-css-classes" />
