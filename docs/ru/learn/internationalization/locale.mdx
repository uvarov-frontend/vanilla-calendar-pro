# Автоматическая локализация

Если ваша локаль поддерживается методом <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toLocaleString" rel="noreferrer" target="_blank">`.toLocaleString()`</a>, вы можете просто передать её в параметр `settings.lang` для локализации календаря.

<Sandbox example="internationalization-locale" />

Если локаль не поддерживается или переведена неправильно, вы всегда можете установить локаль вручную, см. следующий раздел.
