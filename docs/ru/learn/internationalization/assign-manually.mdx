# Ручная локализация

Вы можете ручным образом локализовать календарь, если параметр `settings.lang` не подходит для ваших нужд.
В календаре локализованы только надписи месяцев и дней недели, поэтому вам нужно предоставить соответствующие массивы строк и установить `settings.lang` в `'define'`.

<Info>
  Обратите внимание, что массив недель должен начинаться с воскресенья, это не влияет на **ISO 8601**.
</Info>

<Sandbox example="internationalization-assign-manually" />
